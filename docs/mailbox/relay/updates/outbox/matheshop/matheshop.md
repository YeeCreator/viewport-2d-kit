# support advice (out-down): viewport-2d-kit -> matheshop

## Context

`viewport-2d-kit` supports `matheshop` because `matheshop` depends on `viewport-2d-kit`.

## Adaptation Advice

Describe what `matheshop` should update if `viewport-2d-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
