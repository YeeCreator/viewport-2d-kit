# support advice (out-down): viewport-2d-kit -> complex-system-gallery

## Context

`viewport-2d-kit` supports `complex-system-gallery` because `complex-system-gallery` depends on `viewport-2d-kit`.

## Adaptation Advice

Describe what `complex-system-gallery` should update if `viewport-2d-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
