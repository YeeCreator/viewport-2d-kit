# support advice (out-down): viewport-2d-kit -> autodo

## Context

`viewport-2d-kit` supports `autodo` because `autodo` depends on `viewport-2d-kit`.

## Adaptation Advice

Describe what `autodo` should update if `viewport-2d-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
