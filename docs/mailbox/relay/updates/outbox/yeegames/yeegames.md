# support advice (out-down): viewport-2d-kit -> yeegames

## Context

`viewport-2d-kit` supports `yeegames` because `yeegames` depends on `viewport-2d-kit`.

## Adaptation Advice

Describe what `yeegames` should update if `viewport-2d-kit` changes.

## Suggested Steps

- Required change:
- Compatibility note:
- Validation command:
- Deadline or release note:
